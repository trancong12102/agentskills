# release.yml is generated by `dist init` and regenerated wholesale on every
# config change, so its shell style cannot be fixed here. Everything actionlint
# checks itself still applies to it, and other workflows are unaffected.
paths:
  .github/workflows/release.yml:
    ignore:
      - "shellcheck reported issue in this script.*"
