pre-commit:
  commands:
    oxfmt:
      glob: "*.{md,json,jsonc}"
      run: bunx oxfmt {staged_files}
      stage_fixed: true

    markdownlint-md:
      glob: "*.md"
      run: bunx markdownlint-cli2 --fix {staged_files}
      stage_fixed: true
